<UserControl x:Class="ql_ks.Views.UC_QuanLyNhanVien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
             
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- HEADER -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="QUẢN LÝ NHÂN VIÊN" FontSize="24" FontWeight="Bold" Foreground="#3F51B5" Margin="0,0,0,20"/>
                   
        <!-- FORM PANEL -->
        <materialDesign:Card Grid.Row="1" Grid.Column="0" Margin="0,0,20,0" Padding="15" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Thông tin nhân viên" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>
                
                <TextBox Text="{Binding MaNV, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Mã Nhân Viên (Số)" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Margin="0,0,0,15"/>
                         
                <TextBox Text="{Binding HoTenNV, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Họ và Tên" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Margin="0,0,0,15"/>
                         
                <ComboBox SelectedValue="{Binding GioiTinhNV, UpdateSourceTrigger=PropertyChanged}"
                          SelectedValuePath="Content"
                          materialDesign:HintAssist.Hint="Giới tính" 
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}" 
                          Margin="0,0,0,15">
                    <ComboBoxItem Content="Nam"/>
                    <ComboBoxItem Content="Nữ"/>
                </ComboBox>
                
                <DatePicker SelectedDate="{Binding NgaySinhNV, UpdateSourceTrigger=PropertyChanged}"
                            materialDesign:HintAssist.Hint="Ngày sinh"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                            Margin="0,0,0,15"/>
                         
                <TextBox Text="{Binding SdtNV, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Số Điện Thoại" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Margin="0,0,0,15"/>
                         
                <TextBox Text="{Binding ChucVuNV, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Chức vụ" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Margin="0,0,0,15"/>
                         
                <TextBox Text="{Binding DiaChiNV, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Địa chỉ" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Margin="0,0,0,15"/>
                         
                <DatePicker SelectedDate="{Binding NgayVaoLamNV, UpdateSourceTrigger=PropertyChanged}"
                            materialDesign:HintAssist.Hint="Ngày vào làm"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                            Margin="0,0,0,30"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Button Grid.Row="0" Grid.Column="0" Command="{Binding AddCommand}" Content="Thêm" Margin="5"/>
                    <Button Grid.Row="0" Grid.Column="1" Command="{Binding EditCommand}" Content="Sửa" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    <Button Grid.Row="1" Grid.Column="0" Command="{Binding DeleteCommand}" Content="Xóa" Margin="5" Background="#E74C3C" BorderBrush="#E74C3C" Foreground="White" Style="{StaticResource MaterialDesignFlatDarkBgButton}"/>
                    <Button Grid.Row="1" Grid.Column="1" Command="{Binding ClearCommand}" Content="Làm mới" Margin="5" Style="{StaticResource MaterialDesignFlatButton}"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
        
        <!-- DATAGRID -->
        <materialDesign:Card Grid.Row="1" Grid.Column="1" Padding="10">
            <DataGrid ItemsSource="{Binding ListNhanVien}" 
                      SelectedItem="{Binding SelectedNhanVien}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      IsReadOnly="True"
                      materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã NV" Binding="{Binding MA_NV}"/>
                    <DataGridTextColumn Header="Họ và Tên" Binding="{Binding HoTen_NV}"/>
                    <DataGridTextColumn Header="Chức vụ" Binding="{Binding ChucVu_NV}"/>
                    <DataGridTextColumn Header="SĐT" Binding="{Binding SoDienThoai_NV}"/>
                    <DataGridTemplateColumn Header="Ngày vào làm">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding NgayVaoLam_NV, StringFormat=dd/MM/yyyy}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>

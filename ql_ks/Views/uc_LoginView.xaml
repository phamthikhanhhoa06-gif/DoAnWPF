<UserControl x:Class="ql_ks.Views.uc_LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="20,5,20,15"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="20,5,20,25"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="20,12,20,0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Nền - ĐÃ SỬA: BỎ BACKGROUND="#34495E" Ở ĐÂY -->
    <Grid>

        <!-- Dùng toàn bộ phần này làm nền -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#2C3E50" Offset="0"/>
                <GradientStop Color="#4B5D8A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Form đăng nhập nằm giữa -->
        <Border Width="554" 
                Height="520" 
                Background="White" 
                CornerRadius="12"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">

            <StackPanel Margin="40">

                <!-- Tiêu đề chính -->
                <TextBlock Text="🏨 HỆ THỐNG QUẢN LÝ KHÁCH SẠN" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Margin="0,0,0,10"/>

                <TextBlock Text="ĐĂNG NHẬP / ĐĂNG KÝ" 
                           FontSize="14" 
                           Foreground="#7F8C8D"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- Tên đăng nhập -->
                <TextBlock Text="Tên đăng nhập" Foreground="#555" FontSize="13" Margin="5,0,0,4"/>

                <TextBox Name="txtUserName" 
                         Text="{Binding TenDangNhap, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Mật khẩu -->
                <TextBlock Text="Mật khẩu" Foreground="#555" FontSize="13" Margin="20,18,0,4"/>

                <PasswordBox Name="pssPassword" 
                             PasswordChanged="PssPassword_PasswordChanged"/>

                <!-- Thông báo lỗi -->
                <TextBlock Name="lblMessage" 
                           Text="{Binding ThongBao}" 
                           Foreground="Red" 
                           FontSize="13" 
                           TextWrapping="Wrap" 
                           Margin="20,18,0,0"/>

                <!-- Nút ĐĂNG NHẬP (Xanh) -->
                <Button Content="✅ ĐĂNG NHẬP" 
                        Style="{StaticResource PrimaryButton}"
                        Background="#3498DB" 
                        Foreground="White"
                        Command="{Binding DangNhapCommand}"/>

                <!-- Nút ĐĂNG KÝ (Xanh lá cây) - Mới thêm -->
                <Button Content="📝 TẠO TÀI KHOẢN ĐĂNG KÝ" 
                        Style="{StaticResource PrimaryButton}"
                        Background="#27AE60" 
                        Foreground="White"
                        Click="Register_Click"/>

                <!-- NútThoát -->
                <Button Content="Đóng Ứng Dụng" 
                        Style="{StaticResource PrimaryButton}"
                        Background="#95A5A6" 
                        Foreground="White"
                        Click="CloseApp_Click"
                        Margin="20,15,0,0"/>

            </StackPanel>
        </Border>

        <!-- Nút đóng góc phải trên -->
        <Button Content="⚙️" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Width="35" 
                Height="35" 
                Background="Transparent" 
                Foreground="White" 
                BorderThickness="0" 
                FontSize="14" 
                Margin="10" 
                ToolTip="Cài đặt"/>

    </Grid>
</UserControl>